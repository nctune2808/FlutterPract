enum PropertyType {
  ACCOMMODATION
  PROJECT
  LAND
}

enum PropertyStatus {
  SOLD
  SELLING
  TOSELL
  LET
  LETTING
  TOLET
}

type Property @model @auth(rules: [{allow: public}]) {
  id: ID!
  status: PropertyStatus
  type: PropertyType
  title: String!
  price: Float!
  isUrgent: Boolean
}
