import 'package:hasura_connect/hasura_connect.dart';

String url = 'https://tolo.hasura.app/v1/graphql';
HasuraConnect hasuraConnect = HasuraConnect(url);


// var r = await hasuraConnect.query(docQuery);